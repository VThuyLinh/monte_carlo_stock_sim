[package]
name = "data_io"
version = "0.1.0"
edition = "2024"

[dependencies]
csv = "1.3"
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0"
nalgebra = "0.32" # Để tính toán thống kê (mean, std)
chrono = { version = "0.4", features = ["serde"] }